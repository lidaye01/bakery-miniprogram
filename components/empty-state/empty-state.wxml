<!-- empty-state/empty-state.wxml -->
<view class="empty-state">
  <view class="empty-icon">{{icon}}</view>
  <view class="empty-text">{{text}}</view>
  <button class="empty-btn" wx:if="{{buttonText}}" bindtap="onButtonTap">
    {{buttonText}}
  </button>
</view>
